function cutHalf(number) {
  const half = number / 2;
  return half;
}

const result = cutHalf(100);
console.log(result);

console.log("==========================");

function sum(a, b) {
  const sum = a + b;
  return sum;
}

const result2 = sum(23, 23);
console.log(result2);
